#include <string.h>
/**
 * _strlen - entry point.
 * @s: string.
 * Description: length of a string.
 * Return: length.
 */
int _strlen(char *s)
{
	int length;

	for (length = 0; length <= s[length]; length++)
	{
		length++;
	}
	return (length);
}
