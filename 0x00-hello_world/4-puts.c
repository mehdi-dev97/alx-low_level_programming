#include <stdio.h>

/**
 * main - Print string in console using puts func.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);

}
