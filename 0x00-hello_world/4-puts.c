#include <stdio.h>
/**
 * main - entry point.
 * Description: Print string in console using puts func.
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";
	
	puts(str);
	return (0);
}
