#include <stdio.h>

/**
 * main - Print string in console using printf func.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char str[55] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", str);

	return (0);

}
