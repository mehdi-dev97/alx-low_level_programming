#include <stdio.h>

/**
 * main - problems an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("Infinit loop incoming :(\n");

	i = 0;
	/*
	*while (i < 10)
	*{
	*	putchar(i);
	*}
	*/
printf("Infit loop avoid! \\o/\n");

return (0);
}
