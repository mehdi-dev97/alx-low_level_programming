#include <stdio.h>
/**
 * main - Entry point
 * Description: prints the lowercase alphabet in reverse then new line.
 * Return: Always 0 (success)
 */
int main(void)
{
	char ch;

	for (ch = 'z'; ch >= 'a'; ch--)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
