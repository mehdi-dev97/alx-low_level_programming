#include "main.h"
#include <stdio.h>
/**
 * main - prints its name, followed by a new line.
 * Return: 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[argc * 0]);
	return (0);
}
